- if @category
  - breadcrumb :search, @category
  %span{id: 'category_id', style: 'display:none'}
    = @category.id
- elsif @island
  - breadcrumb :search, @island
  %span{id: 'category_id', style: 'display:none'}
    = @island.id
- elsif @area
  - breadcrumb :search, @area
  %span{id: 'category_id', style: 'display:none'}
    = @area.id
- elsif @genre
  - breadcrumb :search, @genre
  %span{id: 'category_id', style: 'display:none'}
    = @genre.id
- elsif @search_word
  - breadcrumb :search_word, @search_word
